# Gemfile for local development
# No dependencies required for basic server, but you can add gems here if needed

source 'https://rubygems.org'

# Optional: If you want to use Jekyll in the future
# gem 'jekyll', '~> 4.3'

# Optional: If you want to use a different server
# gem 'rack', '~> 2.2'
# gem 'puma', '~> 6.0'
